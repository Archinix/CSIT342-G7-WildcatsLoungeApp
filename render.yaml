services:
  - type: web
    name: wildcats-lounge-backend
    root_dir: WildcatsLoungeBackend/abatayo
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/abatayo-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: 17
